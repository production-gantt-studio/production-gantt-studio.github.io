# Acceptance Evidence — Update 14

| 確認項目 | 結果 |
| --- | --- |
| フェーズ並び替え | PASS：フェーズ設定の上下矢印で、フェーズの表示順を案件データへ保存できる。サイドバー、ガント、月次PDFは同じ順序を使う。 |
| フェーズ削除 | PASS：最後のフェーズを除き、削除前に隣接フェーズへの移管先と件数を確認する。削除後、配下タスクは移管先フェーズへ移る。 |
| 詳細タスク作成 | PASS：タスク行の `+` または詳細パネルの操作から、親タスク直下に詳細タスクを追加できる。 |
| 階層表示 | PASS：詳細タスクは一覧・ガントでインデントと `↳` を使って区別する。親タスクを削除すると、詳細タスクは最上位タスクへ安全に戻る。 |
| 入出力・共有・PDF | PASS：親タスクIDをLocalStorage、共有URL、JSON、CSV、A4月次PDFに保持する。PDFでも親の直下に詳細タスクを配置する。 |
| 型検査・本番ビルド | PASS：`pnpm check` と `pnpm build` が完了した。 |
| 表示 | PASS：通常ガントをデスクトップ幅で確認し、既存のタイムライン、アナウンス、操作列、フェーズ表示を維持している。 |

