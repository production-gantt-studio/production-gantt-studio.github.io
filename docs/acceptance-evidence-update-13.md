# Acceptance Evidence — Update 13

| 確認項目 | 結果 |
| --- | --- |
| 日程未定タスク | PASS：新規タスクは「日程は未定」で追加され、開始・終了日は予定確定まで編集できない。日付付きガントバーではなく `DATE TBC` の未定クリップとして表示する。 |
| 予定確定 | PASS：詳細パネルの「日程は未定」を解除すると、今日を開始・終了の初期値として日程を入力できる。 |
| 重要切替 | PASS：各タスク行の旗ボタンで、詳細パネルを開かずに重要タスクを追加・解除できる。 |
| アナウンス | PASS：未定タスクは期限接近から除外し、重要タスクにした場合は「日程未定」としてアナウンスする。 |
| 既定重要タスク | PASS：テンプレート由来の不要な既定重要指定を除去し、移行済みの保存データでも一度だけ解除する。以後に利用者が指定した重要フラグは保持する。 |
| 保存・共有・出力 | PASS：`isUnscheduled` と `isImportant` はLocalStorage、共有URL、JSON、CSV、A4 PDFのいずれにも含まれる。 |
| 型検査・本番ビルド | PASS：`pnpm check` と `pnpm build` が完了した。 |

