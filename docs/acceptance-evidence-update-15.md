# Acceptance Evidence — Update 15

| 確認項目 | 結果 |
| --- | --- |
| 親タスク自動進捗 | PASS：親タスクは、配下の詳細タスクのうち完了した件数を基に進捗率を自動算出する。親の手動ステータスではなく、詳細タスクの完了状態を集計する。 |
| 表示 | PASS：親タスク行とガントクリップに `0–100%` の集計値を表示し、ガントには進捗を表す細い下部ラインを追加する。 |
| 折りたたみ | PASS：詳細タスクを持つ親タスクには開閉操作を表示する。折りたたむと、一覧・ガントから子タスクが非表示になる。 |
| 保存・共有 | PASS：折りたたみ対象の親タスクIDは案件データとともに保存・共有される。共有ビューでも開閉して閲覧できる。 |
| PDF | PASS：A4月次PDFは親子タスクを展開した状態で出力し、親タスクには自動集計された進捗率を表示する。 |
| 型検査・本番ビルド | PASS：`pnpm check` と `pnpm build` が完了した。 |
| 表示 | PASS：通常ガントの一覧・操作列・タイムラインをデスクトップ幅で確認し、既存の制作資料らしい階層を維持している。 |

