# Acceptance Evidence — Update 04

| 確認項目 | 結果 |
| --- | --- |
| フェーズ表示 | PASS：サイドバーとガント見出しは `ALL`、`PRE-PRODUCTION`、`PRODUCTION`、`POST-PRODUCTION` のみを表示する。 |
| フェーズ編集 | PASS：フェーズ設定パネルでは、英語フェーズ名そのものを編集・保存できる。 |
| サブタイトル | PASS：「企画・設計」「撮影・現場」「編集・納品」および「全体を表示」を画面から除去した。 |
| 既存データ | PASS：旧形式で保存された日本語の既定名は、読み込み時に英語の既定名へ補完する。独自に編集した名称は維持する。 |
| 型検査・本番ビルド | PASS：`pnpm check` と `pnpm build` が完了した。 |

